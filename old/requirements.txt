# requirements.txt — shared base for all versions
librosa>=0.10.0
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0
torch>=2.0.0
tqdm>=4.65.0
matplotlib>=3.7.0
seaborn>=0.12.0

# V1 (CPU only): all of the above, no extras
# V2 (CUDA): all of the above — ensure torch is installed with CUDA support:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# V3 (Colab Pro): deps installed inside notebook cells
